---
import PageLayout from "@/layouts/Base";
import PostPreview from "@/components/blog/PostPreview";
import SocialList from "@/components/SocialList";
import { getAllPosts, sortMDByDate } from "@/utils";

const MAX_POSTS = 10;
const allPosts = await getAllPosts();
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);

const cactusTech: Array<{ title: string; desc: string; href: string }> = [
	{
		title: "Astro",
		desc: "博客框架通过Astro进行构建.",
		href: "https://astro.build",
	},
	{
		title: "Netlify",
		desc: "博客平台部署通过Netlify进行自动部署.",
		href: "https://app.netlify.com/",
	},
	{
		title: "Liuziting",
		desc: "更多内容请访问个人博客站点.",
		href: "https://liuziting.netlify.com/",
	}
];
---

<PageLayout meta={{ title: "Home" }}>
	<section>
		<h1 class="title mb-6">Hi there 👋 welcome!</h1>
		<p class="mb-4">
			Hi, 欢迎来到这个基于Astro构建的个人博客实例站点！
		</p>
		<p class="mb-4">在这里，你可以看到通过Astro构建的博客网站内容模块，涵盖了文章列表、标签分类等丰富的功能。</p>
		<p class="mb-4">如果你想深入了解这些功能是如何实现的，可以点击下面文章查看详细的代码实现和技术细节以及自动化部署的过程。</p>
		<p class="mb-4">同时，我们也欢迎你分享自己的想法和反馈，更多博客内容请访问我的
			<a href='https://liuziting.netlify.com/'
							target="_blank"
							rel="noopener noreferrer"
							class="cactus-link inline-block">个人博客站点！</a></p>
		<SocialList />
	</section>
	<section aria-label="Blog post list" class="mt-16">
		<h2 class="title mb-4 text-xl">Posts</h2>
		<ul class="space-y-4 sm:space-y-2">
			{
				allPostsByDate.map((p) => (
					<li class="flex flex-col gap-x-2 sm:flex-row">
						<PostPreview post={p} />
					</li>
				))
			}
		</ul>
	</section>
	<section class="mt-16">
		<h2 class="title mb-4 text-xl">Links</h2>
		<ul class="space-y-4 sm:space-y-2">
			{
				cactusTech.map(({ href, title, desc }) => (
					<li>
						<a
							href={href}
							target="_blank"
							rel="noopener noreferrer"
							class="cactus-link inline-block"
						>
							{title}
						</a>: 
						<p class="inline-block sm:mt-2">{desc}</p>
					</li>
				))
			}
		</ul>
	</section>
</PageLayout>
